#include "main.h"
/**
 * is_digit - checks for a digit
 * @x: The number to be checked
 * Returns: 1 if it is a digit 0 for anyelse
 */
int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
		return (1);
	}
	return (0);
}
